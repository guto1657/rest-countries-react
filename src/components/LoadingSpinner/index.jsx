import React from 'react';
import { LoadingWrapper, Spinner } from './LoadingSpinnerElements';

export const LoadingSpinner = () => {
  return (
    <LoadingWrapper>
      <Spinner></Spinner>
    </LoadingWrapper>
  );
};
